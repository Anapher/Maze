<UserControl
    x:Class="Tasks.Infrastructure.Administration.Views.CreateTask.CreateCommandView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewModels="clr-namespace:Tasks.Infrastructure.Administration.ViewModels.CreateTask"
    d:DataContext="{d:DesignInstance viewModels:CreateCommandViewModel}"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="300" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <ListBox ItemsSource="{Binding Commands}" SelectedItem="{Binding SelectedCommand}" />
        <ContentControl
            Grid.Column="1"
            Margin="10"
            Content="{Binding CurrentCommandViewModel.View}" />
    </Grid>
</UserControl>